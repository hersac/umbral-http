
out cs: Next {
    pr: called;
    pr: error;

    pu f: Next() {
        th.called = false;
        th.error = null;
    }

    pu f: continue() {
        th.called = true;
    }

    pu f: withError(error) {
        th.called = true;
        th.error = error;
    }

    pu f: wasCalled() {
        r: (th.called);
    }
}
